{
  "name": "ari",
  "version": "1.0.0",
  "description": "A website for a writer",
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "start": "node scripts/start.js",
    "build": "npm run clean && npm run build:scss && npm run build:pug && npm run build:scripts && npm run build:assets",
    "build:assets": "node scripts/build-assets.js",
    "build:pug": "node scripts/build-pug.js",
    "build:scripts": "node scripts/build-scripts.js",
    "build:scss": "node scripts/build-scss.js",
    "clean": "node scripts/clean.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dlangevi",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.3",
    "mongoose": "^6.2.9",
    "nodemon": "^2.0.15",
    "purecss-sass": "^2.1.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.4",
    "concurrently": "^6.3.0",
    "eslint": "^8.12.0",
    "eslint-config-google": "^0.14.0",
    "prettier": "^2.6.1",
    "pug": "^3.0.2",
    "sass": "^1.49.9",
    "shelljs": "^0.8.5",
    "upath": "^2.0.1"
  }
}
